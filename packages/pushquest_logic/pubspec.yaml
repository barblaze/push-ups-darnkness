name: pushquest_logic
description: Pure Dart logic for the PushQuest push-up tracking game.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
